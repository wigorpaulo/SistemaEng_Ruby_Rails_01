<p id="notice"><%= notice %></p>

<p>
  <strong>Descricao:</strong>
  <%= @estado.descricao %>
</p>

<p>
  <strong>Uf:</strong>
  <%= @estado.uf %>
</p>

<p>
  <strong>Pais:</strong>

  <%#= Util.new.consulta_dados(Pai, @estado.id_pais) %>

  <% obj = Pai.new %>
  <%= obj.consulta_nome(@estado.id_pais) %>
  <% obj.freeze %>

</p>

<%= link_to 'Edit', edit_estado_path(@estado) %> |
<%= link_to 'Back', estados_path %>

