<%= form_with(model: tipo_endereco, local: true) do |form| %>
  <% if tipo_endereco.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_endereco.errors.count, "error") %> prohibited this tipo_endereco from being saved:</h2>

      <ul>
      <% tipo_endereco.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :descricao %>
    <%= form.text_field :descricao %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.check_box :status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
