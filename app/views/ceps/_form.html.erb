<%= form_with(model: cep, local: true) do |form| %>
  <% if cep.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cep.errors.count, "error") %> prohibited this cep from being saved:</h2>

      <ul>
      <% cep.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :descricao %>
    <%= form.text_field :descricao %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
